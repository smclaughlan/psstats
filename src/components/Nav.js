import React from 'react';
import { Header, Button } from "grommet";

const Nav = () => {

  return (
    <Header background="brand">
      <Button>Hi</Button>
      <Button>Hi</Button>
      <Button>Hi</Button>
    </Header>
  )
}

export default Nav;
